{% extends 'plantillas/plantilla-auth.html.twig' %}

{% block title %}
¡Inicia sesión!
{% endblock %}

{% block contenido %}
<form method="post" class="form__auth">
  <h3> <span class="text-granota">¡Bienvenido!</span>
    <br> Aquí comienza tu <span class="text-primary">proyecto</span>
  </h3>
  {% if error %}
  <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
  {% endif %}

  {% if app.user %}
  <div class="mb-3">
    Has iniciado sesión como {{ app.user.userIdentifier }}, <a href="{{ path('logout') }}">Cerrar sesión</a>
  </div>
  {% endif %}


  <div class="form-outline form-floating mb-4">
    <input type="text" value="{{ last_username }}" name="username" id="inputUsername"
      class="form-control form-control-lg" autocomplete="username" required autofocus>
    <label for="inputUsername" class="form-label">Usuario</label>
  </div>

  <div class="form-outline form-floating mb-4">
    <input type="password" name="password" id="inputPassword" class="form-control form-control-lg"
      autocomplete="current-password" required>
    <label for="inputPassword" class="form-label">Contraseña</label>
  </div>
  <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

  {#
  Uncomment this section and add a remember_me option below your firewall to activate remember me
  functionality.
  See https://symfony.com/doc/current/security/remember_me.html#}
  <div class="checkbox mb-3">
    <label>
      <input type="checkbox" name="_remember_me"> Recuérdame
    </label>
  </div>

  <div class="pt-1 mb-4">
    <button class="btn btn-granota btn-lg btn-block" type="submit">
      Iniciar sesión
    </button>
  </div>

  <p class="small mb-5 pb-lg-2"><a class="text-muted" href="{{ path('forgot_password_request') }}">¿Has olvidado la contraseña?</a></p>

</form>
{% endblock %}
{% extends 'plantillas/plantilla-auth.html.twig' %}

{% block title %}
Resetea la contraseña
{% endblock %}

{% block contenido %}

{% import 'components/flash.html.twig' as mensajeFlash %}
{% import 'components/boton_volver.html.twig' as botonVolver %}

{{ form_start(form, {'attr': {'class': 'form__auth'}}) }}

<h3><span class="text-granota">Restablece</span> tu contraseña <span class="text-primary">olvidada</span></h3>

{{ mensajeFlash.flashComponent() }}

<div class="form-outline form-floating mb-4">
    {{ form_widget(form.password) }}
    <label for="password" class="form-label">Nueva Contraseña</label>
</div>

<div class="form-outline form-floating mb-4">
    {{ form_widget(form.confirm_password) }}
    <label for="confrim_password" class="form-label">Confirmar Contraseña</label>
</div>

<div class="pt-1 mb-4">
    {{ botonVolver.propiedades('btn btn-primary btn-lg btn-block', path('forgot_password_request')) }}
    {{ form_widget(form.submit) }}
</div>

{{ form_end(form) }}

{% endblock %}
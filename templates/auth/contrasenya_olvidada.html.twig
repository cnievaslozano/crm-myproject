{% extends 'plantillas/plantilla-auth.html.twig' %}

{% block title %}
Has olvidado tu contraseña
{% endblock %}

{% block contenido %}

{% import 'components/flash.html.twig' as mensajeFlash %}
{% import 'components/boton_volver.html.twig' as botonVolver %}

{{ form_start(form, {'attr': {'class': 'form__auth'}}) }}

    <h3>¿Has <span class="text-granota">olvidado</span> tu <span class="text-primary">contraseña</span>?</h3>

    <p>Restablece tu contraseña proporcionándonos tu usuario, automáticamente se te enviará un email en el cual podras restablecer tu clave de acceso</p>

	{{mensajeFlash.flashComponent()}}

    <div class="form-outline form-floating mb-4">
        {{ form_widget(form.usuario)}}
        <label for="usuario" class="form-label">Usuario</label>
    </div>

    <div class="pt-1 mb-4">
        {{ botonVolver.propiedades('btn btn-primary btn-lg btn-block', path('login') ) }}
        {{ form_widget(form.submit) }}
    </div>

{{ form_end(form) }}

{% endblock %}